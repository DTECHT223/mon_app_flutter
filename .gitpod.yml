image: gitpod/workspace-flutter
tasks:
 -init: flutter create . && flutter pub get
  command: flutter run -d web-server --web-port 8080

ports:
 - port: 8080
   onOpen: open-preview
   visibility: public

vscode:
 extentions:
  - dart-code.flutter
  - dart-code.dart-code
 
